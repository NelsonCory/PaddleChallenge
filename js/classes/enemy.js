class Enemy{
    constructor(config){
        this.scene = config.scene;
        this.startX = Math.floor(game.config.width*0.8);
        this.startY = Math.floor(game.config.height/2);

    }
    resetPaddle(){

    }

}